# このプロジェクトについて

大洲市の非公式生活情報サイト「OZU LIFE MEMO」。個人開発・**匿名運営**。

- 公開URL: https://ozulifememo.github.io/ozu-life-memo/ (GitHub Pages、リポジトリはPublic)
- 静的サイト。ビルド工程なし。HTMLを置いてpushすればそれが公開される。
- 規模(2026-08-19時点): 記事129本(`eachnews/`)、大洲と読書18本(`book/`)、大洲の自由研究8本(`jiyu-kenkyu/`)、その他ページ。全170ページ。
- ヘッダー・ナビ・フッター・お問い合わせモーダルは `assets/js/site-chrome.js` の**1ファイルに共通化済み**。ナビを変えるときはここだけ直す(全ページを一括置換しない)。
- 記事の一覧データは `assets/js/news-data.js`。記事を足したらここにも1件足す。
- **記事の管理はレビュー卓ひとつ。** 本人が記事を読み返して ◯(そのまま) / △(直す) / ✕(下げる) と
  メモを付ける非公開アーティファクト。記事案も同じ卓に「記事案」として並ぶ。
  URLと作り直し方は `~/.claude/memory/project_ozu-review-taku.md`。

## 記事の管理にNotionはもう使わない(2026-09-06)

**2026-09-06、本人がNotionのページを全部消した。** 本人の言葉:
「notionはもう、僕みたくない。大洲クイズ以外は使わない。消したい」。
消す前に、Notionにしかないものが無いことは1つずつ確かめてある
(大洲検定131問はサイト側に250問あり、記事も全部サイトとレビュー卓にあった)。

**だから、Notionを見に行こうとしない。**「Notionと本文が違う」という理由で
サイトを直さない。**比べる相手はもう無い。** かつてNotionがやっていた仕事は、
いま全部こちら側にある。

| 前(Notion) | いま |
|---|---|
| 記事の一覧・進捗(①②③④のパイプライン) | **レビュー卓ひとつ** |
| 原稿の置き場所 | `private-notes/genkou/<スラッグ>.md` |
| 「やり直し」「ガチ完了」などの判定 | レビュー卓の ◯ / △ / ✕ とメモ |
| 内容ダブルチェックの記録 | `tools/check-ledger.json`(`ledger.py`) |
| 記事案・ネタの置き場所 | `private-notes/kikaku-ledger.json`(卓に「記事案」として出る) |
| Notionと本文のズレ検査 | **要らなくなった** |

`tools/` にある `html2notion.py` / `md2notion.py` / `notion_diff.py` / `notion_verify.py` /
`check_mojibake.py` は**もう走らせない**。消してはいないので、経緯は
`private-notes/notion-haishi-memo.md` にまとめてある。

## 作業を始める前に

**運営者本人は複数のチャットを同時に走らせることがある。** 作業前に必ず `git status` を見て、見覚えのない変更がないか確認する。あればコミット・ブランチ操作の前に本人に確認する。

## 仕事のやり方はスキルにまとめてある

`.claude/skills/` に4つ置いてある。該当する作業のときは必ず読む。

| スキル | いつ使うか |
|---|---|
| `kiji` | 記事を書く・書き直す |
| `tenken` | サイトを点検する |
| `kokai` | 記事を実際に公開する |
| `getsuji` | 月に一度の定期点検 |

## 絶対に守ること

- **本文に運営者個人が特定できる語を入れない。** 氏名、勤務先、出身校、居住地の詳細、家族の勤務先など。
  名前だけ消しても、生年・出身校といった間接的な手がかりが残れば同じこと。
  **このリポジトリはPublicなので、このファイル自体にも実際の氏名や学校名を書かないこと。**
  具体的にどの語を避けるかは、リポジトリ外の個人メモ（`~/.claude/memory/`）に置いてある。
- **`git add -A` を使わない。** 別チャットの作業中ファイルを巻き込む(実際に起きた)。必ずパスを明示する。
- **`git push` は本人の確認なしに実行しない。**
- **日本語を書くとき `\uXXXX` エスケープを手で組み立てない。** 生の文字をそのまま書き、
  書いた直後に読み返す。16進を1桁書き間違えると、**別の似た漢字に化けたまま気づけない**
  (実例: 沖縄→沖繀、高知→高睑、喜茂別町→喊茅別町、8,500→4,500)。見た目が普通の
  日本語なので、読んでも分からない。**これはNotionだけの話ではない。** 記事HTMLも、
  `tools/*.json` の台帳も、原稿のMarkdownも同じ。日本語を書き込む先は全部これに当たる。
- `docs/` と `private-notes/` は `.gitignore` 済み(個人情報を含むため)。この行を消さない。
- **匿名性は「いまのファイル」だけの話ではない。git の履歴も公開される。**
  2026-09-06、履歴に旧アカウント名の入ったURLが41版、名字が25版(うちコミットメッセージ14件)
  残っていることが分かった。どれもその場では直してあったが、Publicなリポジトリは履歴も
  読めるので消えていなかった。**`check_site.py` の匿名性の検査は「いま追跡しているファイル」
  しか見ない。** 履歴は `python tools/check_history.py` で別に見る。
  このときはリポジトリを作り直した(経緯は `tools/status-state.json` の
  「公開リポジトリの作り直し」)。
- **開発機のローカルパスを、追跡するファイルに書かない。** ドライブ文字から始まるユーザーフォルダの絶対パスや、デスクトップ以下のフォルダ名は、
  どの端末のどのユーザーかの手がかりになる(具体例をここに書くと、この検査自身が鳴るので書かない)。
  書き出し先は引数で受ける。→ 検査「ローカルパス」(関所)

## 完了と言う前に

```
python tools/check_site.py
```

を走らせ、エラーがゼロであることを確認する。使い方は `tools/README.md`。

**これは Stop フック(`.claude/settings.json`)で自動化してある。** 応答を終えようとした瞬間に、いま手を入れたページだけが自動で点検される。ルールから外れていれば終われずに戻される。フックが戻してきたら、直すか、直さない理由を本人に伝えること。黙って無視しない。

「できました」と言う前に実際に動かして確認する。できていないなら「できていない」と正直に伝える。

## 「この記事チェックした?」には記憶で答えない

点検の履歴は `tools/check-ledger.json` に記録してある。聞かれたら台帳を見る。

```
python tools/ledger.py show <スラッグ>
```

`○` = その確認はいまも有効、`×` = 確認後に本文が変わったので無効、
`?` = 確認時の本文が不明(**昔の取り込み分**。いま新しく `?` が付くことはないので、
見かけたら中身を読んで確認し直して `record` する)。

**「もう確認しました」と記憶で答えてはいけない。** 確認したあとに本文を書き直していれば、その確認は無効になっている。台帳はそこを見ている。確認したら `ledger.py record` で必ず記録する。

## 検査は下限を守るもの。文章を機械に寄せるものではない(2026-08-29、本人の直感で確定)

**記事の質が落ちることだけは絶対にダメ。** これは他のどのルールより上にある。

検査には2種類あり、扱いを分ける。

| 種類 | 中身 | 扱い |
|---|---|---|
| **関所** | 匿名性・リンク切れ・画像切れ・文字化け・タグの不整合・出典が2本未満・タイトルの不一致 | 正解が1つしかない。**エラー**として止める |
| **目安** | 1文の長さ・呼びかけ表現・タイトルの行政用語・敬体の混在 | 好みの話。**警告**として見せるだけ。止めない |

フック(`tools/hook_check.py`)は**エラーでだけ止まる**。警告では止まらない。

なぜ分けるか。2026-08-29、1文150字の警告を消すために文を割った結果、「〜することがある」という控えめな言い方が「〜足りなくなる」という断定に変わった。**検査を通すために事実の強さがゆがんだ。** 警告で止める仕組みは「検査に合わせて文章をいじる圧力」を生む。本人の言葉: 「点検をたくさんしすぎると、本当に正しいことだけ言い続けて、一般的なことしか言えなくなる」。

だから:

- **公開済みの記事を、あとから作った目安に合わせて書き直さない。** 目安は新しく書く記事に効かせる。過去の記事に触るのは事実の誤り・関所の違反のときだけ。
- **数字照合(`check_numbers.py`)で「出典に無い」と出た数字を消したり丸めたりしない。** 記事が自分で計算した数字(150個ある)は、このサイトの価値そのもの。照合の目的は「未検証」を見つけることであって「出典の要約以外を消す」ことではない。
- 迷ったら、質を取る。検査を通すために文章を変えたくなったら、その変更で意味・含み・強さが変わっていないかを先に見る。

## 記事が短いときは、中身を足す。ただし水増しは絶対にしない(2026-09-06、本人が明示)

本人の言葉。

> 3000文字以内の記事は内容を増やす。僕の好みにあったこと。文献数を増やす。
> **ただ文章を増やして薄くすることは絶対に許さない。無理なら無理と言ってくれる方がよい。僕が質問を考える。**

`check_site.py` の検査「**文量**」が、**2,800字**に満たない記事をまとめて1件の**警告**で出す。
(最初は3,000字にしたが、同じ日に本人が「もうちょいゆるくしよか。2800文字にしよか」と言ったので下げた。)
**わざとエラーにしていない。** エラーにすると「検査を通すために書き足す」圧力が生まれ、
本人がいちばん嫌がっていることが起きる。2026-08-29の教訓(1文150字の警告を消すために
事実の強さが歪んだ)と同じ構図だからである。

警告が出たときにやることは、次の3つのうちどれか。

1. **本人が知りたがる問いを立てて、調べて足す。** 効くのはこの4つ。
   「なんでそうなったの?」「それって他ではどうなの?」「で、結局どうなったの?」「いくらかかるの? それは適正?」
2. **出典を増やして裏を厚くする。**
3. **これ以上は調べても出てこない、と正直に伝える。**

**3が正解のことがある。** そのときは伸ばさない。何をどこまで探して出てこなかったかを書いて、本人に返す。

**できるだけ大洲に引き寄せる。**(2026-09-06、本人)一般論や全国の話だけで字数を稼がない。
全国の数字を出したら、必ず大洲の数字と並べる。地図・表・自分で計算した数字は、
それ自体が中身なので歓迎される。

## 記事を1本仕上げるときの、最後までの手順(2026-09-05、本人が明示)

**「○○の記事を書いて」と言われたら、公開まで一気にやる。** 途中で止めて確認を求めない。

1. **`/kiji` を読んでから書く。** ガチで書く。出典を積む。数字は自分で計算してよい
2. `python tools/kiji_md.py private-notes/genkou/<スラッグ>.md` でHTMLにする
3. `tools/memou-ledger.json` に吹き出しを書いて `apply_memou.py`
   (見本のまま出すと検査で止まる)
4. `python tools/kaigyo.py --slug <スラッグ>` で改行の箱を入れる
5. `python tools/add_readtime.py`
6. `python tools/make_feed.py`
7. `python tools/check_site.py` で **エラー0**
8. `python tools/check_numbers.py --slug <スラッグ>`
9. `python tools/ledger.py record` で machine / numbers / human を記録
10. **`python tools/build_review.py` → `_review-src.html` をレビュー卓の同じURLに publish**
11. `git add`(**パスを明示**) → `git commit` → `git push`

**10番を忘れない。** レビュー卓は**作った時点の記事のコピー**を中に持っていて、
記事を直しても勝手には追いつかない。2026-09-05、記事を7本直したのに卓が古いままで、
本人が卓を開いて気づいた。**本人が気づくまで分からない形の事故**だった。
いまは `check_site.py` の検査「レビュー卓」が止めるが、止められる前に自分でやること。

**先に聞くのは次の3つだけ。** それ以外は聞かずに最後までやる。

- 新しい記事を**公開**してよいか(書くこと自体は聞かない)
- 公開済みの記事の**題を変える**とき
- **人の名前**や、つらい出来事に触れるとき

## 「いま何が残ってる?」にも記憶で答えない

点検の履歴と同じで、進捗も記憶で答えない。数え直す。

```
python tools/status.py
```

サイトの中のことは全部その場で数える(点検・台帳・git・写真・約束・月間まとめ)。
機械に数えられないものだけ `tools/status-state.json` に「値」と「確認した日」の形で
置いてあり、古くなると `?` が付く。`?` が付いていたら、その項目の**「調べ方」の
とおりに数え直して**、このファイルを更新する。放置しない。

**引き継ぎ.md を新しく作らない。** 2026-09-02時点でリポジトリ直下に10本ある。
数を増やしても、次のチャットはどれが最新か分からない(実際に分からなくなった)。
残ったことは status-state.json に1行足す。引き継ぎ.mdを書くのは、数字では
残せない**経緯**を書くときだけにする。

## ルールは文章ではなく機械に置く

2026-08-29に実測した結果:

- 点検スクリプトが見ているルール … 記事147本中 **違反0本**
- 文章でお願いしているだけのルール … **違反が生き残っていた**

「一度言ったのに直っていない」の原因は、注意する回数ではなく、**そのルールが機械に書かれているかどうか**だった。

だから、**新しいルールを言われたら、まず `tools/check_site.py` に検査として足せないかを考える。** 足したら `--selftest` の `EXPECTED` にも必ず1行足す(足さないと、見ているつもりで見ていない検査になる)。機械にできないもの(疑問形の見出しの答え、裏取りの実質)だけを文章のルールとして残す。

## 調べている最中に見つけたものは、必ず記事案にする(2026-09-06、本人が明示)

本人の言葉。

> 僕がお願いした記事をかくために、参考文献を探すとき、常に
> **これ、これからのガチルールにできる?**
> 無理して記事を作りに行く必要はない。けど、**常にあたまのかたすみにいれること**できる?
> なんか、別のよい記事つくれそうだったら**メモしとこ**的なやつ。

**これはガチルールである。** 1本の記事のために資料を読むと、その過程で
「本題ではないが書けそうなもの」が必ず出てくる。**その場で捨てない。**

やること。

1. 記事を1本仕上げたら、**その調査で出てきた「書けそうなもの」を書き出す**
2. `private-notes/make_kikaku.py` の `IDEAS` に足して、`python private-notes/make_kikaku.py` を走らせる
3. `python tools/build_review.py` → レビュー卓に publish

**無理に記事にしなくてよい。** 案として置くだけでよい。判断は本人がする。

**書くときの形は3つ。** どれも欠かさない。

| 項目 | 中身 |
|---|---|
| `title` | 仮の題。あとで変えてよい |
| `hitokoto` | **クロコからのひと言。** なぜこれを本人に勧めるのか。本人の関心のどこに当たるか |
| `tegakari` | 手がかりと調べ方。次に読む人が最初に見るところ |

**`hitokoto` を手抜きしない。** 本人はここを読んで◯✕を決める。
「面白そうです」では判断できない。**なぜ本人に効くのかを書く。**

## 記事案は、レビュー卓の中に置く(2026-09-06)

本人の言葉。

> 記事案はNotionではなく、僕が記事を見たり、訂正するやつに追加して。
> で、**ちゃんと区別するように。** 記事案に対して、僕が◯か✕をクリックして判定する。
> もちろん、メモもね。**バツにしてもメモをするときがある。バツの理由も教えたら、
> あなたに僕のことを理解してくれるでしょ。**

- 台帳は `private-notes/kikaku-ledger.json`(作るのは `private-notes/make_kikaku.py`)
  ※どちらも `private-notes/` の中。記事案には未公開のネタが並ぶので `.gitignore` 済み
- レビュー卓では **`kind="idea"` の行**として並ぶ。判定・メモ・端末間の同期は記事と同じ仕組み
- 卓の種類チップ「**記事案**」を押したときだけ出る。記事の未読数には混ざらない
- **✕のメモは宝物である。** 本人がなぜ興味を持てないかが書いてある。
  次に案を作るときは、必ず先に✕のメモを読む

## 何が刺さって、何が刺さらないか(2026-09-06、本人が明示)

本人の言葉。

> **自然が苦手。** 僕の苦手、興味ないものは**生物、自然の話と歴史**。
> だから、**自然×歴史の話はなにもそそられない。**
> **綺麗な景色は好き。神南山とか登るの好き。大きな自然を見ることはとてもすき。
> 高いところから街を見るのは大好き。**

2026-09-06、記事を7本書いたが、城の瓦・松枯れ病・冨士山の寄付・うかいの鵜・
国営農地・野佐来の田んぼと、**自然と歴史に寄りすぎていた**。
出典も数字もしっかりしていたが、**本人の関心から外れていた**。ネタ帳から
「手応えが強い順」で選ぶと、**調べやすさ(会議録に残っている量)で選んでしまう。**

| | |
|---|---|
| **刺さる** | お金の損得／制度の仕組み／働くこと／教育／**言葉**(ゆる言語学ラジオの愛聴者)／生活の実感／**高いところからの眺め** |
| **刺さらない** | 生物・自然そのもの／歴史(城・藩主・文化財)／**自然×歴史はいちばん遠い** |

**「景色」と「自然」は別である。** 生態系や樹木の話は遠いが、
**「どこから街が見えるか」は近い。** 入口を生活・お金・制度・眺めに
置き換えられるかを、選ぶ前に考える。

## 今日ある道具(2026-09-06時点)

記事を書く・直すときに使うもの。

| 道具 | 何をするか |
|---|---|
| `tools/kiji_md.py` | 原稿(Markdown)をHTMLにする。**見出し・箇条書き・引用(`> `)・表(`\| \|`)** に対応 |
| `tools/apply_kahitsu.py` | **既存の記事に書き足す。** `private-notes/genkou/<スラッグ>-add.md` を読んで本文を足し、出典を足し、「調べた資料 N本」を数え直す |
| `tools/apply_memou.py` | メモうの吹き出しを入れる。踊り場は台帳の `aside` に `"before": "見出しの一部"` で位置を固定できる |
| `tools/kaigyo.py` | 題と吹き出しの改行の箱(`<span class="nb">`)を入れる。**`apply_memou.py` のあとに必ず走らせる**(先に走らせると箱が消える) |
| `tools/check_history.py` | **git の履歴に個人が特定できる語が無いかを見る。** `check_site.py` はここを見ない |
| `tools/gikai.py` | 市議会の会議録を全文検索する(307回分、平成20年3月〜令和8年6月) |

## 出典のPDFから文字が取れないとき(2026-09-06)

紙をスキャンしたPDFや、表を線と字形で描いたPDFは、文字が1字も入っていない。
`check_numbers.py` では「出典が取れず」になり、その出典で裏付けた数字が全部
「出典に無い」と出る。やり方はこう。

1. `curl` にブラウザのUAと `-e https://www.city.ozu.ehime.jp/` を付けて落とす
2. `fitz` の `get_pixmap(dpi=170)` でPNGにする
3. **Readツールで目で読む**
4. 書き起こして `tools/_source_cache/<URLのsha1>.txt` に置く

大洲市サイトは叩きすぎると全体が403になる。25〜30秒あける。
403が出ても消えたとは限らないので、`https://archive.org/wayback/available?url=…`
で生死を確かめる。
