# デザイン改修の引き継ぎ ｜ 記事改稿チャット宛て

デザイン改修チャット(コミット ef090bc 以降)から、記事改稿チャットへの引き継ぎです。
2026-08-19時点。

## 1. 最重要: 表記統一(77本)は壊れていない。書き戻し不要

ef090bc が eachnews/ の129ファイルを触ったため「表記統一が生き残っているか不明」との懸念が出たが、
**129記事すべてについて、変更前後の本文テキストを機械照合し、差分ゼロを確認済み**。

ef090bc が記事HTMLに対して行ったのは、次の構造変更**だけ**:

1. `<head>` にWebフォント読み込み3行を追加(`data-ozu-fonts` が目印)
2. 冒頭の日付行 `<p class="article-date">` から先頭の掲載日を除去、カテゴリ名を `<span class="article-cat">` 化
3. メイン写真 `<figure class="article-hero-img">` を記事内容に合う写真へ差し替え(または削除)
4. 記事末尾のランダム写真 `<figure class="article-random-photo">` と `random-photo.js` を削除(全記事)
5. 出典ボックス `<div class="content-block source-box">` を本文冒頭 → 末尾へ移動。
   見出しを `<h2 class="source-box-title">` に変更し、掲載日の行 `<p class="source-box-posted">` を追加
6. 「あわせて読みたい最新ニュース」→「同じテーマの記事」に変更

本文の段落・太字・言い回しには一切触れていない。

**依頼: 「再変換して差分が出たら書き戻す」はやらないでください。**
差分は上記1〜6が原因で必ず出ますが、それは意図した変更です。
書き戻すとデザイン改修が消えます。本文に直したい箇所がある場合は、ファイル全体の書き戻しではなく該当段落だけを編集してください。

## 2. news-data.js のタイトル1件を修正した

B班改稿コミット(a379761 ほか)のあと、`nagahama-umetate-pabukome` の title が
`nagahama-tsunami-takasa` と同一(津波の記事のタイトル)になっていた。コピペミスと思われる。
記事HTML側のh1は正しかったので、news-data.js を h1 に合わせて復元済み:

> 長浜港埋立て、総事業費92.8億円。パブリックコメント64件が映した賛否

改稿時は「news-data.js の title と記事の h1 が一致しているか」を確認項目に足すことを推奨。

## 3. ファイルの分担

| ファイル | 管轄 |
|---|---|
| eachnews/*.html の本文段落 | 記事改稿チャット |
| news-data.js | 共有(記事側: 記事の追加・title等 / デザイン側: 触らない。今回のタイトル復元は例外) |
| photos-data.js, article-images.js, article-card.js, article-related.js, site-chrome.js | デザイン側 |
| style.css, index.html, news/index.html, link/index.html, photo/index.html | デザイン側 |
| tools/*.js, 撮影リスト.md | デザイン側 |

## 4. 今後、記事を新規作成・全文再生成するときのルール

- **ランダム写真は廃止済み**。`data-random-photo` や `random-photo.js` を新記事に入れない
  (`assets/js/random-photo.js` はもう存在しない)
- 出典ボックスは本文の**末尾**(「同じテーマの記事」の直前)に置く
- 冒頭の日付行に掲載日を書かない(掲載日は出典ボックス内の1行)
- メイン写真は手で書かず、`assets/js/article-images.js` にひもづけを書いて
  `node tools/fix-articles.js <slug>` を実行する(冪等・何度でも実行可)
- 万一テンプレートから全文を再生成した場合は、後で必ず
  `node tools/fix-articles.js` と `python tools/add-webfonts.py` を実行する(両方とも冪等)

## 5. 従来ルールの再確認

- 日本語は生の文字で書く。`\uXXXX` エスケープの手組みは禁止(D班の文字化け8件の原因)
- 書き込み後は読み返して文字化け確認をしてから完了報告
