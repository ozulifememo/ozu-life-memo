# 引き継ぎ：点検の仕組みづくりと記事8本の公開（2026-08-30）

このチャットでやったことと、残っていること。次のチャットはまずここと `CLAUDE.md` を読む。
（Publicリポジトリなので、運営者個人が分かる語はこのファイルにも書かない）

---

## 1. 今日できあがった仕組み（すべてcommit・push済み）

| もの | 場所 | ひとこと |
|---|---|---|
| 機械点検 | `tools/check_site.py` | 関所(エラー)と目安(警告)を分けた。`--selftest` の EXPECTED は19項目 |
| 止めるフック | `.claude/settings.json` → `tools/hook_check.py` | 応答を終える瞬間に、触ったページだけ点検。エラーでだけ止まる |
| 数字照合 | `tools/check_numbers.py` | 記事の数字が出典に載っているか。`--slug` / `--json` / `--render` / `--index` / `--watch` |
| 点検台帳 | `tools/ledger.py` + `tools/check-ledger.json` | 「この記事チェックした?」は記憶でなくここを見る。`show <slug>` / `stale` / `record` |
| 文字化け検知 | `tools/check_mojibake.py` | Notionに書いた後の読み返し用。今日また出た字(圈蠟戗櫔儒)を辞書に追加 |
| ルール | `CLAUDE.md`「検査は下限を守るもの」「ルールは文章ではなく機械に置く」 | **記事の質を落とさないことが最上位**。過去記事を新しい目安で書き直さない |

スキル4本（`/kiji` `/tenken` `/kokai` `/getsuji`）も上の道具を使う手順に更新済み。

## 2. 今日公開した記事8本（HTML・台帳・sitemap・Notion③・commit・push・実機確認まで完了）

kanshin-breaker / nankotsu-dendo-earphone / kishou-bosai-advisor / kyushoku-center-yoryoku /
ozu-shinrin-kankyozei-tsukue / smart-shrink-ozu / ozu-taxi-rideshare / ozu-haze-no-hosomichi

- 台帳には「人が読んで裏取り」「数字と出典の照合」の両方を記録済み（`python tools/ledger.py show <slug>` で○が並ぶ）。
- Notion「②記事作成中」の8行は **進捗状態=見送り** にしてクロコメモに③のURLを書いた。**②の行の削除は本人の手作業**（MCPに削除機能がない）。

## 3. 今日見つかった不具合と対処（次のチャットが知っておくべきこと）

1. **数字照合の出典キャッシュが3分の1化けていた**（915本中346本）。大洲市サイトなど charset を返さないページを requests が ISO-8859-1 で読んでいた。`check_numbers.py` を直し、化けたキャッシュは削除した。
   → **過去の「出典に無い数字」判定の一部は、この化けが原因の誤判定の可能性がある。** 取り直してから再照合すること。
2. **大洲市サイト(www.city.ozu.ehime.jp)が同日403を返すようになった**（curl・requests・Playwrightいずれも。短時間に取りすぎたためと思われる）。日を改めて `python tools/check_numbers.py --index` で取り直す。
3. Notionへ書くときにツール引数に `\uXXXX` を手で組んで、2本で漢字が化けた。**JSON引数でも生の日本語を書く。書いた直後に notion-fetch で読み返す。** 今回は読み返しで見つけて直した。

## 4. 残っていること（優先順）

1. 大洲市サイトの403が解けたら: `python tools/check_numbers.py --index` → `python tools/check_numbers.py --json 結果.json` → 「見つからず」が残る記事を人手で確認（`python tools/ledger.py todo --level numbers` で未照合の記事が出る）。
2. `python tools/ledger.py stale` に出る記事の再確認（いま1件: jinko-47nen-graph の数字照合。出典を1本足したので本文ハッシュが変わっただけ）。
3. 今日HTMLだけ直した記事（1文分割・呼びかけ表現・改題1本）のNotion③本文が古いまま。`python tools/html2notion.py <slug>` で起こして同期する。対象は `git log --stat 0d764ae~3..0d764ae` あたりの eachnews を見る。
4. `HANDOFF-16kiji.md` は**別チャット**の引き継ぎ（16本の執筆予定）。このチャットとは無関係。`撮影リスト.md` の変更も別チャット。

## 5. 再開のしかた

```
git status --short          # 見覚えのない変更がないか
python tools/check_site.py  # エラー0を確認
python tools/ledger.py stale
```

記事を書くなら `/kiji`、公開するなら `/kokai`、点検なら `/tenken`。
